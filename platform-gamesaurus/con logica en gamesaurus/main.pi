class main implements GL_Program {
    virtual start(){
        Setbackcolor(RGB(0,0,0));
        Setresolution(800,640);
        Setviewscale(-2);
        //Arrancamos un vgs que tiene un stage con un mapa
        LoadVGS("game.vgs");
        RunStage("stage1","Stage1");
    }
}
  